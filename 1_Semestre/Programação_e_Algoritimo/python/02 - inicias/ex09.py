dolares = float(input('digite quantos dolares você tem: '))
cotacao = float(input('digite a cotação do dolar hoje: '))

real = dolares * cotacao

print(f'a converção de ${dolares} para real é R${real}')